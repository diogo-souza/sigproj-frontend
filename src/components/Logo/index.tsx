import React from 'react';
import * as S from './styles';

// temp until I get the svg image
const Logo: React.FC = () => (
  <S.Container>
    <h1>Logo</h1>
  </S.Container>
);

export default Logo;
